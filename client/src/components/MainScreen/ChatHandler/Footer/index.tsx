import { StyledFooter } from "./Styles"

export const Footer = () => {
  return (
    <StyledFooter>
      <p>This app is powered by OpenAi API, keep in mind that you are holding a conversation with an AI.</p>
    </StyledFooter>
  )
}