import styled from 'styled-components'

export const MainContainer = styled.div`
  .bi-gear-fill {
    font-size: var(--px-4);
    cursor: pointer;

    &:hover {
      color: var(--color-primary);
    }
  }
`